{
  "name": "larena/dataview",
  "description": "Larena Dataview: Universal data presentation layer that can render the same structured dataset as table, kanban, calendar, gantt, cards, tree or other reusable views without reimplementing the same UI for every package.",
  "type": "library",
  "license": "Apache-2.0",
  "authors": [
    {
      "name": "Rim Zabarov",
      "homepage": "https://github.com/zabarov"
    }
  ],
  "require": {
    "php": "^8.3",
    "larena/core": "dev-main",
    "larena/property": "dev-main"
  },
  "autoload": {
    "psr-4": {
      "Larena\\Dataview\\": "src/"
    }
  },
  "autoload-dev": {
    "psr-4": {
      "Larena\\Dataview\\Tests\\": "tests/"
    }
  },
  "require-dev": {
    "phpstan/phpstan": "^2.0"
  },
  "repositories": [
    {"type": "path", "url": "../core", "options": {"symlink": true}},
    {"type": "path", "url": "../property", "options": {"symlink": true}}
  ],
  "scripts": {
    "test": [
      "@php -d zend.assertions=1 -d assert.exception=1 tests/Unit/DataviewContractTest.php",
      "@php -d zend.assertions=1 -d assert.exception=1 tests/Unit/DataviewFailsClosedTest.php",
      "@php -d zend.assertions=1 -d assert.exception=1 tests/Unit/ReadOnlyDataviewTableAdapterTest.php",
      "@php -d zend.assertions=1 -d assert.exception=1 tests/Unit/DataviewTableRuntimeTest.php",
      "@php -d zend.assertions=1 -d assert.exception=1 tests/Unit/DataviewSixViewRuntimeTest.php",
      "@php -d zend.assertions=1 -d assert.exception=1 tests/Unit/DataviewDescriptorNormalizerTest.php",
      "@php -d zend.assertions=1 -d assert.exception=1 tests/Integration/DataviewDescriptorPersistenceTest.php",
      "@php tests/Unit/MinimalCmsDependencyContractTest.php"
    ],
    "test:unit": [
      "@test"
    ],
    "lint": "php scripts/lint.php",
    "analyse": "php scripts/analyse.php",
    "validate:larena": "php scripts/validate-larena-package.php",
    "evidence:check": "php scripts/check-evidence.php",
    "scope:check": "php tools/larena-scope-check.php",
    "quality:gate": [
      "@validate:larena",
      "@lint",
      "@analyse",
      "@test",
      "@evidence:check",
      "@scope:check"
    ]
  },
  "config": {
    "sort-packages": true,
    "allow-plugins": {},
    "platform": {"php": "8.3.31"}
  },
  "minimum-stability": "dev",
  "prefer-stable": true
}
